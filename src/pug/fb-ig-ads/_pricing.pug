section.pricing#pricing
  .container
    .row
      .col-md-12
        header
          h2 Cada negocio es diferente, conversemos
          p Cada organización tiene objetivos distintos. Por eso es importante que te comuniques con nosotros. Charlá e intercambiá ideas con un experto en marketing digital sobre cuál es la estrategia de marketing digital más conveniente para tu negocio. Te prepararemos un presupuesto en menos de 24 horas para que podamos trabajar juntos.
        .row
          .col-sm-10.col-sm-offset-1
            form.row(method="", action="")
              .col-sm-6
                .input-group
                  label(form='name') Nombre y apellido
                  input(type="text", placeholder='' id='name' required)
                .input-group
                  label(form='email') Email
                  input(type="email", placeholder='' id='email' required)
                .input-group
                  label(form='phone') Teléfono de contacto
                  input(type="text", placeholder='' id='phone' required)
              .col-sm-6
                .input-group
                  label(form='textarea') ¿En qué te podemos ayudar?
                  textarea(id='textarea' required)
              button.btn.btn_orange.submit(type='submit') Enviar consulta
