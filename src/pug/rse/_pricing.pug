section.pricing#pricing
  .container
    .row
      .col-md-10.col-md-offset-1
        header
          h2 Certificados SSL
          p Elegí el certificado ssl que más se adapte a tu sitio web
        .row
          .col-xs-12
            .pricing-table
              .pricing-item.table-1
                header
                  h4 SSL Wildcard
                section
                  ul
                    li Hasta #[span 100 subdominios]
                      svg(class="icon info")
                        use(xlink:href="#info")
                    li Compatibilidad navegadores #[span 100%]
                      svg(class="icon info")
                        use(xlink:href="#info")
                    li Costo de instalación #[span incluído] 
                      svg(class="icon info")
                        use(xlink:href="#info")
                footer
                  p Al año #[span $3199]
                  a.btn(href="", title="") Comprar

              .pricing-item.pricing-item_featured.table-6
                header
                  h4 SSL Estándar
                section
                  ul
                    li #[span 1] dominio
                      svg(class="icon info")
                        use(xlink:href="#info")
                    li Compatibilidad navegadores #[span 100%]
                      svg(class="icon info")
                        use(xlink:href="#info")
                    li Costo de instalación #[span incluído] 
                      svg(class="icon info")
                        use(xlink:href="#info")
                footer
                  p Al año #[span $699]
                  a.btn(href="", title="") Comprar

              .pricing-item.table-12
                header
                  h4 SSL Free
                section
                  ul
                    li #[span 1] dominio
                      a(href="#0", title="", data-tooltip="Agrega todos los dominios que desees a tu cuenta de web hosting.")
                        svg(class="icon info")
                          use(xlink:href="#info")
                    li Compatibilidad navegadores #[span 90%]
                      a(href="#0", title="", data-tooltip="No limitamos el espacio siempre y cuando sean contenidos para tu sitio web.")
                        svg(class="icon info")
                          use(xlink:href="#info")
                    li Costo de instalación #[span $400] 
                      a(href="#0", title="", data-tooltip="Crea todas las cuentas de email que quieras, sin costo adicional.")
                        svg(class="icon info")
                          use(xlink:href="#info")
                footer
                  p Al año #[span $399]
                  a.btn(href="", title="") Comprar

        .row
          .col-xs-12
            p.iva *Al precio hay que incluirle el IVA 21%
        .row
          .col-xs-12
            p.allPlans Precios anuales excepto el SSL Free que es el precio de instalación por única vez.
    
  
