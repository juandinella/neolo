section.hero.hero-section.heroSmall.heroDomian.custom-background(data-background='img/hero-domain.jpg', data-background-position='50% 33%')
  .container
    .row
      .col-md-10
        .hero-whiteBox
          header
            h1 Acá va el título de Whois
            h2 Creo que podria tener un h2 porque sino queda medio pobre
          section
            form(method="", action="")
              .input-group
                input(type="text", placeholder='' required)
                button.btn.btn_orange(type='submit') Buscar
